cosnt fs = require("fs");

const input = fs.readFileSync("input.txt", "utf-8").trim().split("\n");

for (const line of input) {
  const result
  const remainingDigit = 12

  for (const digit of line) {
    
  }
}